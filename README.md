# Revenue Challenge


## Requirements

* Docker

## Installation

1. Build docker image:

    ```docker compose build```

2. Up the containers:

    ```docker compose up -d```

3. Run unit tests

    ```docker compose exec revenue bash

    pytest
    ```
